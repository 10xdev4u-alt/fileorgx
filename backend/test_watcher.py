from core.watcher import FileWatcher
import logging

# Basic test for the watcher integration
if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    print("Watcher module loaded successfully.")
