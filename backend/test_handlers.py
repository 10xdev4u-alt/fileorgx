from utils.file_handlers import FileHandler
import os

# Basic test for file handlers
def test_handlers():
    print("File handlers module loaded.")
    # More tests will be added when dependencies are confirmed

if __name__ == "__main__":
    test_handlers()
